<%= link_to "New Picture", "/pictures/new" %>
<h1>Pictures</h1>

<% @pictures.each do |picture| %>
	<% picture_name = "#{picture.title}, #{picture.artist}" %>
	<p><%= picture_name %></p>
	<%= link_to image_tag(picture.url, alt: picture_name), picture_path(picture) %>
<% end %>